services:
  simkes:
    container_name: simkes
    image: mfathoor/simkes-api:alpine
    ports:
      - ${APP_PORT}:${APP_PORT}
    networks:
      simkes:
    restart: always

networks:
  simkes:
    external: true
